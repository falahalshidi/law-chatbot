# إعداد كلمة مرور مشفرة للمدير

## ✅ تم تحديث النظام

تم تحديث النظام لتشفير كلمات المرور باستخدام **PBKDF2** مع **SHA-256**. الآن:
- ✅ كلمات المرور مشفرة قبل الحفظ في قاعدة البيانات
- ✅ لا يمكن رؤية كلمات المرور في قاعدة البيانات
- ✅ استخدام Web Crypto API للتشفير الآمن

## 🔧 تحديث كلمة مرور المدير

لأن حساب المدير الافتراضي تم إنشاؤه قبل إضافة التشفير، يجب تحديث كلمة المرور:

### الطريقة 1: استخدام صفحة HTML (الأسهل)

1. افتح الملف `scripts/update-admin-password.html` في المتصفح
2. أدخل كلمة المرور الجديدة (مثلاً: `admin123`)
3. اضغط "إنشاء Hash"
4. انسخ الأمر SQL الذي يظهر
5. اذهب إلى Supabase Dashboard → SQL Editor
6. الصق الأمر SQL وقم بتنفيذه

### الطريقة 2: استخدام Supabase SQL Editor مباشرة

1. اذهب إلى Supabase Dashboard
2. افتح SQL Editor
3. أنشئ حساب مدير جديد بكلمة مرور مشفرة:

```sql
-- أولاً، احذف الحساب القديم إن وجد
DELETE FROM users WHERE email = 'admin@example.com';

-- ثم أنشئ حساب جديد (ستحتاج إلى hash كلمة المرور أولاً)
-- استخدم scripts/update-admin-password.html لإنشاء hash
```

### الطريقة 3: إنشاء حساب جديد من الواجهة

1. افتح التطبيق
2. اذهب إلى صفحة "إنشاء حساب جديد"
3. أنشئ حساب جديد بالبريد: `admin@example.com`
4. بعد الإنشاء، اذهب إلى Supabase SQL Editor
5. نفذ هذا الأمر لجعل الحساب مدير:

```sql
UPDATE users 
SET is_admin = true, is_approved = true 
WHERE email = 'admin@example.com';
```

## 🔐 كيفية عمل التشفير

- **الخوارزمية**: PBKDF2 مع SHA-256
- **التكرارات**: 100,000 (آمن جداً)
- **Salt**: عشوائي لكل كلمة مرور
- **التنسيق**: `salt:hash` (base64)

## ⚠️ ملاحظات مهمة

1. **لا يمكن استرجاع كلمة المرور**: كلمات المرور مشفرة بشكل أحادي الاتجاه
2. **كل كلمة مرور لها salt مختلف**: حتى لو كانت كلمات المرور متطابقة، الـ hash مختلف
3. **آمن**: يستخدم معايير أمنية عالية (PBKDF2 مع 100,000 تكرار)

## 🧪 اختبار النظام

1. أنشئ حساب جديد من صفحة "إنشاء حساب جديد"
2. سجل الدخول (بعد موافقة المدير)
3. تأكد من أن كلمة المرور لا تظهر في قاعدة البيانات

## 📝 تحديث المستخدمين الحاليين

إذا كان لديك مستخدمين موجودين بكلمات مرور غير مشفرة:

1. اطلب منهم تغيير كلمة المرور من صفحة تسجيل الدخول
2. أو احذف حساباتهم واطلب منهم التسجيل مرة أخرى

---

**النظام الآن آمن! 🔒**

