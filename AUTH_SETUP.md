# إعداد نظام المصادقة والإدارة

## ✅ ما تم إنجازه

1. **قاعدة البيانات**: تم إنشاء جدول `users` في Supabase مع الحقول التالية:
   - `id`: معرف فريد
   - `email`: البريد الإلكتروني (فريد)
   - `password_hash`: كلمة المرور (يجب تشفيرها في الإنتاج)
   - `is_admin`: هل المستخدم مدير؟
   - `is_approved`: هل تمت الموافقة على المستخدم؟
   - `created_at`: تاريخ الإنشاء
   - `updated_at`: تاريخ التحديث

2. **صفحة الهبوط**: صفحة رئيسية جميلة مع:
   - نص على اليسار يشرح الموقع
   - أزرار تسجيل الدخول وإنشاء حساب على اليمين
   - تصميم أبيض وأسود مثل الموقع الحالي

3. **صفحات المصادقة**:
   - صفحة تسجيل الدخول
   - صفحة إنشاء حساب جديد
   - التحقق من حالة الموافقة قبل السماح بالدخول

4. **صفحة الإدارة**:
   - عرض جميع المستخدمين المسجلين
   - الموافقة/رفض الوصول للمستخدمين
   - إحصائيات عن المستخدمين

## 🔑 حساب المدير الافتراضي

تم إنشاء حساب مدير افتراضي:
- **البريد الإلكتروني**: `admin@example.com`
- **كلمة المرور**: `admin123`

**⚠️ مهم**: يجب تغيير كلمة المرور في الإنتاج!

## 🚀 كيفية الاستخدام

### 1. تشغيل التطبيق

```bash
npm install
npm run dev
```

### 2. تسجيل الدخول كمدير

1. اذهب إلى `http://localhost:5173`
2. اضغط على "تسجيل الدخول"
3. استخدم:
   - البريد: `admin@example.com`
   - كلمة المرور: `admin123`

### 3. إدارة المستخدمين

1. بعد تسجيل الدخول كمدير، سيتم توجيهك إلى صفحة الإدارة
2. يمكنك رؤية جميع المستخدمين المسجلين
3. اضغط على "الموافقة" للموافقة على مستخدم
4. اضغط على "رفض" لرفض وصول مستخدم

### 4. إنشاء مستخدم جديد

1. اذهب إلى صفحة "إنشاء حساب جديد"
2. املأ البيانات
3. بعد الإنشاء، يجب أن يوافق المدير على الحساب قبل إمكانية تسجيل الدخول

## 🔧 إعدادات Supabase

المفاتيح موجودة في `src/lib/supabase.ts`:
- **URL**: `https://tfilijyzvjnsbltqyono.supabase.co`
- **Anon Key**: موجود في الكود

يمكنك تغييرها عبر متغيرات البيئة:
```env
VITE_SUPABASE_URL=your_url_here
VITE_SUPABASE_ANON_KEY=your_key_here
```

## ⚠️ ملاحظات أمنية مهمة

1. **كلمات المرور**: حالياً يتم حفظ كلمات المرور كنص عادي. في الإنتاج، يجب استخدام `bcrypt` أو `argon2` لتشفير كلمات المرور.

2. **RLS Policies**: تم تعطيل Row Level Security للتبسيط. في الإنتاج، يجب تفعيلها وإعداد السياسات بشكل صحيح.

3. **Service Role Key**: لا تضع Service Role Key في الكود الأمامي. استخدمه فقط في الخادم.

## 📝 تغيير حساب المدير

لإنشاء أو تغيير حساب مدير، يمكنك:

1. تسجيل الدخول إلى Supabase Dashboard
2. الذهاب إلى SQL Editor
3. تنفيذ:

```sql
-- تحديث مستخدم موجود ليكون مدير
UPDATE users 
SET is_admin = true, is_approved = true 
WHERE email = 'your-email@example.com';

-- أو إنشاء مدير جديد
INSERT INTO users (email, password_hash, is_admin, is_approved)
VALUES ('your-email@example.com', 'your-password', true, true);
```

## 🎨 التصميم

- **الألوان**: أبيض وأسود (مثل الموقع الحالي)
- **الاتجاه**: دعم كامل للغة العربية (RTL)
- **التجاوب**: يعمل على جميع الأجهزة

## 📁 الملفات الجديدة

- `src/components/landing-page.tsx` - صفحة الهبوط
- `src/components/login-page.tsx` - صفحة تسجيل الدخول
- `src/components/signup-page.tsx` - صفحة إنشاء حساب
- `src/components/admin-page.tsx` - صفحة الإدارة
- `src/components/chat-page.tsx` - صفحة الشات (محمية)
- `src/components/protected-route.tsx` - مكون حماية المسارات
- `src/lib/supabase.ts` - إعدادات Supabase

