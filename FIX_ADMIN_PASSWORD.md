# إصلاح كلمة مرور المدير

## 🔧 المشكلة

كلمة مرور المدير في قاعدة البيانات هي `PLACEHOLDER_UPDATE_ME` وليست hash صحيح، لذلك لا يمكن تسجيل الدخول.

## ✅ الحل المؤقت

تم تحديث صفحة تسجيل الدخول لدعم كلمات المرور القديمة مؤقتاً. يمكنك الآن تسجيل الدخول باستخدام:
- **البريد**: `admin@example.com`
- **كلمة المرور**: `admin123`

## 🔐 الحل الدائم (يجب تنفيذه)

لإصلاح المشكلة بشكل دائم وإنشاء hash صحيح:

### الطريقة 1: استخدام صفحة HTML

1. افتح `scripts/generate-admin-hash.html` في المتصفح
2. أدخل كلمة المرور: `admin123` (أو أي كلمة مرور تريدها)
3. اضغط "إنشاء Hash وتحديث قاعدة البيانات"
4. انسخ الأمر SQL الذي يظهر
5. اذهب إلى Supabase Dashboard → SQL Editor
6. الصق الأمر SQL وقم بتنفيذه

### الطريقة 2: إنشاء حساب جديد

1. افتح التطبيق
2. اذهب إلى صفحة "إنشاء حساب جديد"
3. أنشئ حساب جديد بالبريد: `admin@example.com` وكلمة المرور: `admin123`
4. اذهب إلى Supabase SQL Editor
5. نفذ هذا الأمر:

```sql
UPDATE users 
SET is_admin = true, is_approved = true 
WHERE email = 'admin@example.com';
```

## ⚠️ ملاحظة مهمة

بعد تحديث hash كلمة المرور في قاعدة البيانات، سيتم إزالة الدعم المؤقت لكلمات المرور القديمة، وسيعمل النظام بشكل آمن تماماً.

---

**الآن يمكنك تسجيل الدخول! 🎉**

